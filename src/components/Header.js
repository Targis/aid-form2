import Typography from '@mui/material/Typography'

const Header = () => {
  return (
    <Typography
      variant="h4"
      component="h1"
      sx={{
        margin: '2rem 0',
      }}
    >
      Header
    </Typography>
  )
}

export default Header
